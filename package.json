{
  "name": "pi-extension-creator",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "description": "A pi.dev extension creator and validator for guiding LLM-driven extension workflows.",
  "type": "commonjs",
  "main": "dist/cli.js",
  "bin": {
    "pi-extension-creator": "dist/cli.js"
  },
  "pi": {
    "extensions": [
      "./src/extension"
    ],
    "prompts": [
      "./prompts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "validate": "npm run build && node dist/cli.js validate",
    "bootstrap": "npm run build && node dist/cli.js bootstrap",
    "self-install": "npm run build && node dist/cli.js bootstrap",
    "validate:src": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@types/node": "^20.19.40"
  }
}
